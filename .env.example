# Non-Human Resources (NHR) Configuration
# Most users don't need any env vars - the relay handles everything.

# Only needed if you're running your own relay instance:
# NHR_RELAY_URL=https://your-own-relay.workers.dev/complain

# Only needed for the relay worker (Cloudflare secret, not in .env):
# TG_BOT_TOKEN=your-telegram-bot-token
